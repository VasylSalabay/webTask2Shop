@using Domain.Entities
@model IEnumerable<Book> 

@{
    ViewBag.Title = "Book";
}

@foreach (Book book in Model)
{
    <div>
        <h3>@book.Name</h3>
        <p>@book.Author</p>
        <h4>@book.Price.ToString("# uah")</h4>
    </div>
    <hr />
}

